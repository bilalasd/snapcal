# Production bundle env — the ONLY env source for release builds.
# scripts/archive.sh exports these as real environment variables, which beat
# every .env* file in Expo's precedence, so a stray .env.local can never leak
# dev values into a store build.
#
# ⚠️ CLERK KEY IS STILL THE DEV INSTANCE (pk_test). TestFlight-internal is fine;
# scripts/archive.sh --production refuses to build until this is pk_live.
# Create the production instance in the Clerk dashboard, paste the key here.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_bXVzaWNhbC1yZWluZGVlci05Ni5jbGVyay5hY2NvdW50cy5kZXYk
EXPO_PUBLIC_API_URL=https://mealio-api-five.vercel.app
